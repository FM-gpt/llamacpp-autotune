# 4f9a41c — baseline (Qwen2.5-14B Q4_K_M @ d=2048)

The starting incumbent. Full GPU offload + flash attention, default batch/ubatch,
f16 KV cache. Workload fixed at `p=512 n=256 d=2048` (loaded-context regime).

**Result:**
- gen_tok_s: 29.17 ± 0.29
- prompt_tok_s: 830.29 ± 27.22
- peak_vram: 10.08 GB (delta ~9.0 GB over idle) — within the 11 GB budget, ~2 GB headroom
- perplexity: n/a (bit-exact baseline)

**Decision: keep** (it's the baseline — everything is measured against this).

**Notes:** at 10 GB the card is nearly full, so VRAM budget, KV-cache quant (`-ctk/-ctv`),
and partial offload (`-ngl`) are all real levers from here. Generation noise is tiny
(±0.29 tok/s), so the keep rule can resolve sub-tok/s real differences.
