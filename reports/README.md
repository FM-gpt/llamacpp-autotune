# reports

`python scripts/run_with_report.py ...` writes Markdown reports and JSON sidecars here.

Commit reports that justify a model/config promotion or document an important failed experiment. For noisy exploratory sweeps, commit a summary plus representative reports instead of every throwaway run.
